import React, { useState } from 'react';

const useDropdown = (label, defaultState, options) => {
  const [state, setState] = useState(default );
  const id = ùse - dropdown - ${ label.replace(" ", "").toLowerCase()
}`
}

export default useDropdown;